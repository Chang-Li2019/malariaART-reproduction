# Regenerated ESM-3 feature cache (33.4 GB for the full panel) — see README Tier 2
features/
*_features.pkl

# Large 56-gene DMS outputs (260 MB) — the unpublished extension, not paper
# observations. Distribute via a data archive (e.g. Zenodo), not git.
results/mutagenesis_56genes/dms/
results/mutagenesis_56genes/rankscore/

# NOTE: data/raw/sequences/ (33 MB, 167 genes) IS tracked — it is needed to
# reproduce the Fig 4 single-variant benchmark and the gene-panel verify check.

# Local outputs
*.log

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
