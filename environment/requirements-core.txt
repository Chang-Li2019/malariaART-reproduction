# Core dependencies — everything except regenerating ESM-3 features.
#
# Covers: verify/ (Tier 0), and Tier 1 scripts 00, 01, 03, 04, 06, the two
# postprocess scripts, and convert_supplementary_to_csv. No GPU, no torch, no esm.
#
# Requires Python >= 3.10 (the code uses `X | None` type hints at runtime).
#
# Install into a fresh environment:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r environment/requirements-core.txt
#
# Versions are pinned to the ones the published results were produced with, so
# retraining (03_train_classifiers) reproduces exactly. Tier 0 (verify) and the
# read-only scripts tolerate newer releases if you need to relax these.

numpy==1.26.4
pandas==2.1.4
scipy==1.15.0
scikit-learn==1.6.0
PyYAML==6.0.2

# Only needed to re-run convert_supplementary_to_csv.py; the CSV exports it
# produces are already shipped in data/published_tables/supplementary_csv/.
openpyxl>=3.1
